{
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile --config.node-linker=isolated",
  "buildCommand": "npx prisma migrate resolve --applied 20260507000000_add_game_waitlist; npx prisma migrate deploy && npx prisma generate && next build",
  "crons": [
    {
      "path": "/api/cron/send-reminders",
      "schedule": "0 12 * * *"
    }
  ]
}
